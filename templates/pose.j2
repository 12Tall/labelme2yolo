path: '{{ root_dir }}'
train: 'train'
val: 'val'
test: 'test'

{# | 管道运算符 #}
kpt_shape: [{{ key_points | length }},{{ dim }}]
flip_dix: {{ range(key_points | length) | list }}

names: 
{% for name in names %}  {{loop.index0}}: {{ name }}
{% endfor %}